# Spring



### Spring Boot

> ###### Spring Boot 是 Spring 提供的一个子项目 ，用于快速构建 Spring 应用程序
>
> ###### 特性：
>
> - 起步依赖：一个 ' spring-boot-starter-web ' 的Maven坐标整合了完成一个功能需要的所有坐标
> - 自动配置：遵循约定大于配置的规则，boot程序启动后，bean 对象自动注入到 IoC 容器中
> -   其      他：内嵌的Tomcat、Jetty 、不需要 XML配置（yml、properties）
>
> ###### 手动创建Spring Boot工程：

###### 